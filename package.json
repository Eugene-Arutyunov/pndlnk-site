{
  "scripts": {
    "prebuild": "rm -rf _site",
    "build": "eleventy",
    "build:clean": "eleventy",
    "dev": "eleventy --serve",
    "projects:build-json": "node scripts/build-projects-json.js"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "devDependencies": {
    "csv-parse": "^5.6.0"
  }
}
