{% macro caseInitiativeMeta(parentUrl, parentTitle, initiativeTitle) %}
<div class="ids__space XL"></div>
<div class="case-page__header">
<a href="{{ parentUrl }}">← {{ parentTitle }}</a>
</div>
<div class="ids__space XS"></div>
<h1>{{ initiativeTitle }}</h1>
<div class="ids__space M"></div>
{% endmacro %}
